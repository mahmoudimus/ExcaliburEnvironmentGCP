# vim: ts=2
---
- hosts: GCP
  
  vars:
    vHome: /home/student002_bics_lu
      
  tasks:

  - include_role:
      name: getDesktop

  - include_role:
      name: shell_tools
    vars:
      packages:
        - p7zip-full
        - cmatrix
        - cowsay

  - include_role:
      name: Excalibur
    vars:
      version: minimal
